# configx 上下文

本文件定义 `configx` 与其使用方共享的核心词汇。它只记录领域含义与能力边界，
不记录具体实现、API 签名、存储或部署决定。

## 角色与边界

**分层配置存储**：把若干配置源按注册顺序合并成一份不可变快照的进程内组件；它不做类型化
schema 推导，也不是分布式配置中心或远端 secret manager。
_Avoid_: 配置中心（「中心」暗示服务端与多节点一致性，本仓库是纯进程内库）

**存储门面**（`ConfigxStore`）：持有全部源与当前快照的唯一入口，读取与替换都要经它进行；
它不是全局单例，实例之间的关系由调用方自行决定。
_Avoid_: 全局配置（本仓库没有进程级全局实例，也没有隐式共享）

**配置源**（`ConfigSource`）：能一次性给出 `key → value` 集合的提供者，如内存、带前缀环境
变量或 `KEY=VALUE` 文件；它不负责优先级，也不参与合并规则。
_Avoid_: provider（容易与「含重试、凭证轮换的远端服务」混淆）

**快照**：`reload` 后对外的不可变视图（`Arc<BTreeMap<String, String>>`），可廉价克隆且按键
有序；它不随源的变化自动更新。
_Avoid_: 视图（`subset_snapshot` 等派生结果才是视图，快照本身是权威副本）

## 合并与优先级

**注册顺序**：源的相对优先级，**后注册者覆盖先注册者**；它不是数值优先级，也不可事后重排。
_Avoid_: 权重（权重暗示可调数值，本仓库只有顺序）

**层**：一次注册所代表的一个配置来源；合并时只按层序覆盖，不做逐键的深合并或类型合并。
_Avoid_: 覆盖组（本仓库没有分组概念，层与源是一一对应的）

**空快照**：全部源合并后不含任何键的结果；`allow_empty_snapshot = false` 时视为冲突并保留
旧快照，而不是接受一份空配置。
_Avoid_: 初始状态（空快照是一个需要判定的结果，而不是「尚未加载」）

**键校验**：键必须非空、不含控制字符且不超过 512 字节；除此之外不做任何规范化。
_Avoid_: 键清洗（本仓库拒绝非法键，但不修剪、不折叠大小写）

## 并发与订阅

**单写多读**：读取用 `&self` 且可并发；`reload` 与 `register_source` 需要 `&mut self`，
借用检查器本身就是并发边界。
_Avoid_: 线程安全（过于笼统；本仓库的具体约束是「读并发、写独占」）

**generation（变更序号）**：单调递增的版本号，订阅者据此判断是否需要重读快照；它不是时间戳，
也不承诺与真实时钟相关。
_Avoid_: 版本号（本仓库的「版本」不表达兼容性，只表达「变过几次」）

**订阅**（`ConfigSubscription`）：订阅者侧的一次等待句柄，产生 `Changed` / `TimedOut` /
`Closed` 三种结果；它不携带变更内容，只表明状态可能已更新。
_Avoid_: 回调（本仓库是拉模型，不注册回调函数）

**等待结果**（`ConfigWaitOutcome`）：一次等待的完整分类——变更、超时或通知端已关闭；它不区分
「哪些键变了」，那是 `diff_snapshots` 的职责。
_Avoid_: 事件（「事件」常暗示带载荷，这里只有状态转移）

## 脱敏与失败语义

**脱敏**：把 `secret:` 前缀键的值在展示路径上替换为 `***` 的动作；它只作用于 `Debug` 与日志，
不改变任何读取结果。
_Avoid_: 加密（脱敏不改变存储中的真实值，也不提供任何保密性）

**读取永不脱敏**：`ConfigxStore::get` 永远返回原始值的约定，与「脱敏只作用于展示路径」互为
一体的两面。
_Avoid_: 按需脱敏（没有任何开关能让 `get` 返回脱敏值）

**失败不改状态**：源加载或校验失败时，当前快照与 `generation` 保持原样的约定。
_Avoid_: 回滚（本仓库根本没有变更中间态，因此谈不上回滚）

**可重试**（`ConfigxError::Unavailable`）：在「至少一个源尚未成功加载」时由 `ping` 返回的
分类；它与「配置本身非法」是两类不同失败。
_Avoid_: 网络错误（成因可能是文件缺失或配置为空，不一定是网络）
